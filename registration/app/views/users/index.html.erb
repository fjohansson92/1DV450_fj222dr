<h1>Alla användare</h2>
<%= will_paginate %>
	<ul id="users">	
		<% @users.each do |user| %>
			<li>
				<%= link_to "#{user.name} (#{user.email})", user %>
			</li>
		<% end %>
	</ul>	
<%= will_paginate %>
